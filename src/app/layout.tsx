import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Hookflow -- Visual Webhook & API Pipeline Builder",
  description:
    "Create automated workflows with webhooks, scheduled triggers, and API actions. A lightweight automation tool for developers.",
  openGraph: {
    title: "Hookflow -- Webhook & API Pipeline Builder",
    description:
      "Build automated workflows connecting triggers to actions. Webhooks, scheduled tasks, HTTP requests, and more.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
